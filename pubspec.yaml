name: appspraow
description: "Modern AI ChatBot mobile application with clean architecture."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3

  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6
  firebase_storage: ^11.5.6
  firebase_analytics: ^10.7.4
  firebase_crashlytics: ^3.4.8

  # Authentication
  google_sign_in: ^6.1.6

  # HTTP & API
  dio: ^5.4.0
  retrofit: ^4.0.3
  json_annotation: ^4.8.1

  # Navigation
  go_router: ^12.1.3

  # UI & Animations
  lottie: ^2.7.0
  flutter_animate: ^4.3.0
  shimmer: ^3.0.0
  cached_network_image: ^3.3.0

  # File Handling
  file_picker: ^6.1.1
  image_picker: ^1.0.4
  path_provider: ^2.1.1
  permission_handler: ^11.1.0

  # Payments
  flutter_stripe: ^9.5.0
  in_app_purchase: ^3.1.11

  # Utilities
  uuid: ^4.2.1
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.1
  package_info_plus: ^4.2.0
  connectivity_plus: ^5.0.2
  dartz: ^0.10.1

  # Localization
  flutter_localizations:
    sdk: flutter

  # Icons & UI
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting
  flutter_lints: ^5.0.0

  # Testing dependencies
  mockito: ^5.4.4
  fake_cloud_firestore: ^2.4.1+1
  firebase_auth_mocks: ^0.13.0

  # Build & Code Generation
  build_runner: ^2.4.13
  riverpod_generator: ^2.6.5
  json_serializable: ^6.8.0    # 👈 متوافق مع riverpod_generator
  retrofit_generator: ^10.0.2
  custom_lint: ^0.5.7
  riverpod_lint: ^2.3.7

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/
    - assets/icons/
